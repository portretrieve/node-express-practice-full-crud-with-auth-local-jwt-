<h1 class="container text-center">Clothing List</h1>
<hr>
<div class="row container">
    <div class="col">
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">Index</th>
                    <th scope="col">Name</th>
                    <th scope="col">Price</th>
                </tr>
            </thead>
            <tbody>
                <!-- Repeating Row -->
                <% for (let i = 0; i < clothingList.length; i++) { %>
                    <tr>
                        <th scope="row"><%= i + 1 %></th>
                        <td><%= clothingList[i].name %></td>
                        <td><%= clothingList[i].price %></td>
                        <td class="text-center"><a href="/clothing-list/edit/<%= clothing[count]._id %>" class="btn btn-primary btn-sm edit"><i class="fas fa-edit fa-sm"></i> Edit</a></td>
                    </tr>
                    <% } %>
            </tbody>
        </table>
    </div>
</div>